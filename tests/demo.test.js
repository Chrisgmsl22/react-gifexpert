import { render } from "@testing-library/react";
import { AddCategory } from "../src/components";

describe('Name ', () => {
    test('should first', () => { 
        render(<AddCategory/>);
     });
})
